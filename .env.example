# 预留：后端 API 基址（接入时启用）
# VITE_API_BASE_URL=

# 预留：是否使用 Mock（接入时启用）
# VITE_USE_MOCK=true

# 预留：默认界面语言（当前仓库仅内置 zh-CN）
# VITE_DEFAULT_LOCALE=zh-CN

# Stitch 设计稿拉取（仅 npm run stitch:* 脚本需要；勿提交真实密钥）
# STITCH_API_KEY=
