node_modules
dist
*.local
.env
.env.*.local
.DS_Store
node_modules/.tmp

# Local IDE / Cursor（不提交工作区配置；共享 AI 规则保留 .cursor/rules/）
.cursor/*
!.cursor/rules/

# Stitch HTML 拉取（体积大、可本地再生）
docs/stitch/fetched/*.html
